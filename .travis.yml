language: node_js
node_js:
  - 12
directories:
  - node_modules
env:
  - CI=true
install: npm ci
script: skip

jobs:
  include:
    - stage: test & lint
      script: npm run test:nowatch
      script: npm run lint
    